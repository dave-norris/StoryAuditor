# Database URL - Set by Railway automatically
# For local development, use .env.local instead
# This file is committed to git, so don't put secrets here
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/storyauditor"
